load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "pages",
    allow_js = True,
    declaration = True,
    incremental = True,
    preserve_jsx = True,
    resolve_json_module = True,
    tsconfig = "//:tsconfig",
    deps = [
        "//components",
        "//lib",
        "//types",
        "@npm//@types/react",
        "@npm//next",
    ],
)
