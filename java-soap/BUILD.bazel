# This tool used to be included in older versions of the JDK,
# but is now a standalone program.
java_binary(
    name = "wsimport",
    main_class = "com.sun.tools.ws.WsImport",
    runtime_deps = ["@maven//:com_sun_xml_ws_jaxws_ri"],
)
