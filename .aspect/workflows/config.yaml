tasks:
  - checkout:
      update_strategy: rebase
  - lint:
  - format:
  - test:
      bazel:
          flags:
            - --config=rbe
            - --action_env=FOO=bar
notifications:
  github: {}
