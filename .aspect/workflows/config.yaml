# See https://docs.aspect.build/workflows/configuration
tasks:
  - checkout:
      update_strategy: rebase
  - lint:
  - format:
  - test:
  - delivery:
      auto_deliver: false
      rules:
        - condition:
            only_on_change: false
            branches:
              - ^delivery$
notifications:
  github: {}
