# For details on buf.yaml configuration, visit https://buf.build/docs/configuration/v2/buf-yaml
#
# To get 'buf' and plugins on the PATH, run:
# bazel run tools:bazel_env
# pnpm install
# Then you can:
# buf dep update
# buf generate
version: v2
modules:
  - path: protobuf
deps:
  - buf.build/bufbuild/protovalidate
lint:
  use:
    - BASIC
    - PROTOVALIDATE
breaking:
  use:
    - FILE


